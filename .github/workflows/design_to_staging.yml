name: Sync design_staging into staging (excluding protected files/folders)

on:
  push:
    branches:
      - design_staging

permissions:
  contents: write  # Required for authenticated push

jobs:
  sync-design:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout design_staging branch
        uses: actions/checkout@v4
        with:
          ref: design_staging
          path: design_staging

      - name: Checkout staging branch
        uses: actions/checkout@v4
        with:
          ref: staging
          path: staging

      - name: Copy all design files to staging root (exclude backadm-dmc, .htaccess, etc)
        run: |
          rsync -av \
            --exclude='backadm-dmc/' \
            --exclude='.htaccess' \
            --exclude='laravel-debug.php' \
            --exclude='startup.sh' \
            design_staging/ staging/

      - name: Commit and push to staging branch
        run: |
          cd staging
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Auto-sync from design_staging to staging on $(date)" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.STAGING_GH_PAT }}@github.com/${{ github.repository }} HEAD:staging
